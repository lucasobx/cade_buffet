<!DOCTYPE html>
<html>
  <head>
    <title>CadeBuffet</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <%= link_to root_path do %>
        <h1>Cade Buffet?</h1>
      <% end %>
      <nav>
        <% if owner_signed_in? %>
          <%= current_owner.description %> |
          <%= link_to 'Cadastrar Novo Buffet', new_buffet_path %>
          <%= link_to 'Sair', destroy_owner_session_path, data: { turbo_method: :delete } %>
        <% else %>
          <%= link_to 'Entrar', new_owner_session_path %>
        <% end %>
      </nav>
    </header>
    <main>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      <%= yield %>
    </main>
  </body>
</html>
