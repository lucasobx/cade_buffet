<!DOCTYPE html>
<html>
  <head>
    <title>CadeBuffet</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  </head>

  <body class="bg-dark text-light">
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #111;">
        <div class="container-fluid">
          <div class="navbar-brand">
            <%= link_to root_path do %>
              <h1>CadeBuffet</h1>
            <% end %>
          </div>
          
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>

           <div class="collapse navbar-collapse" id="navbarScroll">
            <ul class="navbar-nav me-auto my-2 my-lg-0 navbar-nav-scroll" style="--bs-scroll-height: 100px;">
              <% if client_signed_in? %>
                <li class="nav-item">
                  <div class="nav-link disabled">
                    <%= current_client.description %>
                  </div>
                </li>
                <li class="nav-item">
                  <div class="nav-link">
                    <%= link_to 'Meus Pedidos', client_orders_path %>
                  </div>
                </li>
                <li class="nav-item">
                  <div class="nav-link">
                    <%= link_to 'Sair', destroy_client_session_path, data: { turbo_method: :delete } %>
                  </div>
                </li>
              <% elsif owner_signed_in? %>
                <li class="nav-item">
                  <div class="nav-link disabled">
                    <%= current_owner.description %>
                  </div>
                </li>
                <li class="nav-item">
                  <div class="nav-link">
                    <%= link_to 'Pedidos', buffet_orders_path %>
                  </div>
                </li>
                <li class="nav-item">
                  <div class="nav-link">
                    <%= link_to 'Meu Buffet', current_owner.buffet if current_owner.buffet.present? %>
                  </div>
                </li>
                <li class="nav-item">
                  <div class="nav-link">
                    <%= link_to 'Sair', destroy_owner_session_path, data: { turbo_method: :delete } %>
                  </div>
                </li>
              <% else %>
                <li class="nav-item">
                  <div class="nav-link">
                    <%= link_to 'Entrar Como Cliente', new_client_session_path %>
                  </div>
                </li>
                <li class="nav-item">
                  <div class="nav-link">
                    <%= link_to 'Dono de Buffet', new_owner_session_path %>
                  </div>
                </li>
              <% end %>
            </ul>

            <div class="d-flex" role="search">
              <%= form_with url: search_buffets_path, method: :get do |f| %>
                <%= f.label :query, 'Buscar Buffet' %>
                <%= f.text_field :query %>
                <%= f.submit 'Buscar' %>
              <% end %>
            </div>
          </div>
        </div>
      </nav>
    </header>
    <main class="container">
      <div class="row justify-content-center">
        <div class="col-md-8">
          <p class="notice"><%= notice %></p>
          <p class="alert"><%= alert %></p>
          <%= yield %>
        </div>
      </div>
    </main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  </body>
</html>
