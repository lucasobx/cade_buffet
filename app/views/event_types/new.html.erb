<div class="container mt-5">
  <h1 class="text-center">Novo Tipo de Evento</h1>

  <div class="row justify-content-center">
    <div class="col-md-8">
      <%= render 'form' %>

      <div class="text-center mt-4">
        <%= link_to 'Voltar', buffet_path(@buffet.id), class: 'btn btn-secondary' %>
      </div>
    </div>
  </div>
</div>